public class Node<T> {
    int key; // key to find a particular node
    T data; // actual data that should be stored in the node
    Node leftChild; // left child of the node
    Node RightChild; // right child of the node
}
